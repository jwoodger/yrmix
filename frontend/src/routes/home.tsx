import React from 'react';
import { Alert, Container } from 'react-bootstrap';

const Home: React.FC = () => {
	return (
		<Container>
			<Alert variant="dark">Hello!</Alert>
		</Container>
	);
};

export default Home;
